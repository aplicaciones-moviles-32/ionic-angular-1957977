import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.misstagram.app',
  appName: 'Misstagram',
  webDir: 'www',
  bundledWebRuntime: true
};

export default config;
